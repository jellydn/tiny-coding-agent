{
  "name": "tiny-agent",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "dev": "bun --watch index.ts",
    "generate:skills": "bun run scripts/generate-embedded-skills.ts",
    "build": "bun run generate:skills && bun build index.ts --compile --outfile=tiny-agent",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bun run tsc --noEmit",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "prepare": "husky",
    "release:patch": "bun run test && bun run typecheck && bun run lint && bunx bumpp patch --yes",
    "release:minor": "bun run test && bun run typecheck && bun run lint && bunx bumpp minor --yes",
    "release:major": "bun run test && bun run typecheck && bun run lint && bunx bumpp major --yes",
    "pre": "bun run test && bun run typecheck && bun run lint"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "@modelcontextprotocol/sdk": "^1.25.2",
    "ink": "^6.6.0",
    "ink-box": "^2.0.0",
    "ink-spinner": "^5.0.0",
    "ollama": "^0.6.3",
    "openai": "^6.16.0",
    "react": "^19.2.3",
    "tiktoken": "^1.0.15",
    "yaml": "^2.8.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.8",
    "bumpp": "^10.4.0",
    "husky": "^9.1.7",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.39.0",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
